## 1. Banner compartido (prerequisito de Home Blocks)

- [x] 1.1 Mover el schema de `src/payload/blocks/page/Banner.ts` a `src/payload/blocks/shared/Banner.ts` (mismo objeto `Block`, mismo `slug: 'banner'`) y actualizar el import en `Pages.ts` — verificar con `pnpm typecheck` y que `Pages.layout` sigue incluyendo `Banner` sin cambios de comportamiento
- [x] 1.2 Crear `src/components/sections/banner-section.tsx` (`BannerSection`, presentacional, recibe `title/description/image/link/variant` resuelto) — verificar que no importa nada de `payload` ni de `src/payload/`

## 2. Payload schema: Home Global y Home Blocks

- [x] 2.1 Crear `src/payload/blocks/home/HeroNews.ts` con `eyebrow/headline/description/ctaLabel/ctaLink/contentMode(manual|automatic)` y campos condicionales `mainPost/secondaryPosts` (manual) o `sourceCategory/limit` (automatic) — verificar en Payload Admin que los campos condicionales aparecen/desaparecen según `contentMode`
- [x] 2.2 Crear `src/payload/blocks/home/CategoryExplorer.ts` con `title/categories(hasMany)/showViewAll/viewAllLabel`
- [x] 2.3 Crear `src/payload/blocks/home/LatestPosts.ts` con `title/limit(default 6)/category(opcional)/layout(grid|list|mixed)`
- [x] 2.4 Crear `src/payload/blocks/home/PostsByCategory.ts` con `title/category(required)/limit/layout(grid|horizontal|featured-grid)/showViewAll`
- [x] 2.5 Crear `src/payload/blocks/home/FeaturedPosts.ts` con `title/posts(hasMany)/layout(grid|carousel|editorial)`
- [x] 2.6 Crear `src/payload/blocks/home/VideoFeature.ts` con `title/source(post|external)/post/videoURL/thumbnail/headline/description` y condicionales por `source`
- [x] 2.7 Crear `src/payload/globals/Home.ts`: `layout(blocks: [HeroNews, CategoryExplorer, LatestPosts, PostsByCategory, FeaturedPosts, VideoFeature, Banner])`, `seo` opcional, `versions: { drafts: true }`, `access: { read: () => true, update: isAdmin }` — verificar que un `writer` no puede actualizar el Global (test de acceso o prueba manual en Admin)
- [x] 2.8 Registrar `Home` en `payload.config.ts` (`globals: [Navigation, Footer, SiteSettings, Home]`) — verificar que `Home` aparece en el menú de Payload Admin
- [x] 2.9 (Aprobado durante revisión visual manual — ver `docs/60-segundos-spec.md` §19.2) Crear `src/payload/blocks/home/EditorialIntro.ts` con `headlinePrimary`/`headlineAccent`/`description`/`backgroundImage`/`foregroundImage`/`ctaLabel`/`ctaLink` (todos requeridos; `ctaLink` usa el mismo patrón de CTA plano ya usado por `HeroNews`/`Banner`, resuelto vía `resolveLink()` — no un modelo nuevo) — sin campos de posición, color, CSS, variante visual, mapa o galería separados
- [x] 2.10 Agregar `EditorialIntro` al array `blocks` de `Home.layout` en `src/payload/globals/Home.ts` — `pnpm typecheck` confirma el schema válido; verificación visual en Payload Admin (los 8 tipos ofrecidos, reordenable libremente) pendiente en la Tarea 14.8

## 3. Migración y tipos generados

- [x] 3.1 Correr `pnpm migrate:create` contra la base de datos de desarrollo y revisar el SQL generado antes de commitear — verificar que el par `.json`/`.ts` aparece en `src/payload/migrations/` (SQL revisado y editado a mano: el generador incluyó de más las tablas de `Navigation`/`Footer`/`SiteSettings`, ya existentes en la base de desarrollo por push mode desde Fase 5; se removieron del `up`/`down` para dejar la migración escrita solo con lo que realmente introduce este change)
- [x] 3.2 Verificar la cadena completa de migraciones (`initial_schema` → `add_media_uploaded_by` → `home_global`) contra una base PostgreSQL limpia y desechable (no la de desarrollo, que es push-managed): las 3 corrieron en orden, `payload migrate:status` las reporta `Ran: Yes`, y `\dt home*`/`\d home` confirman las tablas y foreign keys del Global `Home` y sus blocks. Base desechable destruida después; la de desarrollo no se tocó
- [x] 3.3 Correr `pnpm generate:types` y commitear el `src/payload-types.ts` regenerado — verificar que `Home`, `HeroNewsBlock`, `CategoryExplorerBlock`, etc. aparecen como tipos exportados
- [x] 3.4 Correr `pnpm migrate:create` para el delta de `EditorialIntro` (migración nueva y separada de `20260910_071432_home_global`, que no se reescribe) y revisar el SQL generado — confirmado: `20260910_165003_editorial_intro.ts` solo contiene `home_blocks_editorial_intro`/`_home_v_blocks_editorial_intro` (tablas, FKs, índices), sin volver a incluir Navigation/Footer/SiteSettings; `migrations/index.ts` actualizado automáticamente
- [x] 3.5 Verificar la cadena completa de migraciones (incluida la nueva) contra una base PostgreSQL limpia y desechable — NO contra la base de desarrollo push-managed — corriendo desde cero y confirmando `payload migrate:status` con todas en `Ran: Yes`; destruir la base desechable después — verificado: `CREATE DATABASE migration_verify_60segundos_v2`, las 4 migraciones corrieron en orden, `\d home_blocks_editorial_intro` confirma columnas/FKs, `migrate:status` reporta las 4 en `Ran: Yes`, base desechable destruida, base de desarrollo (`60segundos`, ledger con solo la fila `dev`) sin tocar
- [x] 3.6 Regenerar `src/payload-types.ts` y confirmar que `EditorialIntroBlock` aparece como tipo exportado en `Home['layout']` — confirmado

## 4. Data Access Layer

- [x] 4.1 Crear `src/lib/data/home.ts` con `getHome()` (usa `findGlobalPublished('home', <depth suficiente>)`) — verificar que la función existe y usa el helper compartido, sin un segundo helper de acceso a Payload
- [x] 4.2 Añadir `getPostsByCategory({ categoryId, limit })` a `src/lib/data/posts.ts`, usando `findPublished` con `where: { and: [{_status:{equals:'published'}}, {or:[{primaryCategory:{equals:categoryId}},{additionalCategories:{contains:categoryId}}]}] }`, `select:{content:false}` — verificar con un Post real cuya categoría configurada está solo en `additionalCategories` que aparece en el resultado

## 5. Validación de proveedor de video externo

- [x] 5.1 Crear `src/lib/editorial/video-provider.ts` con `resolveExternalVideoUrl(url): { provider: 'youtube'|'vimeo'; embedId: string } | undefined`, con allowlist de hostnames (`youtube.com`, `youtu.be`, `vimeo.com`, `player.vimeo.com`) — verificar con casos de prueba: URL válida de YouTube, URL válida de Vimeo, URL de un dominio no permitido (debe devolver `undefined`), URL malformada (debe devolver `undefined`, sin lanzar)

## 6. Resolver de Home

- [x] 6.1 Crear `src/lib/home/resolve-home-blocks.ts` con una función de resolución por tipo de bloque y un `resolveHomeBlocks(layout)` que las orquesta, devolviendo una unión discriminada frontend-safe — verificar que ningún resolver importa un componente de `src/components/` (solo `import type` de `BannerData`/`ArticleCardData`/`CategoryCardData`, mismo patrón ya usado por los view-models de Fase 5; sin import en tiempo de ejecución)
- [x] 6.2 Implementar el resolver de `HeroNews`: normaliza `manual`/`automatic` a `{ mainPost, secondaryPosts }`, filtrando cualquier relación manual que llegue como `typeof === 'number'` (no publicada) vía `isPopulated()` — verificación funcional con datos reales pendiente en la Tarea 14.2
- [x] 6.3 Implementar el resolver de `CategoryExplorer`: mapea `categories` vía `mapCategoryToCategoryCardData` ya existente, excluyendo categorías no pobladas vía `isPopulated()`
- [x] 6.4 Implementar el resolver de `LatestPosts` y `PostsByCategory`: usan `getLatestPosts`/`getPostsByCategory` y `mapPostToArticleCardData` — ambas queries usan `select:{content:false}` (heredado de `findPublished`)
- [x] 6.5 Implementar el resolver de `FeaturedPosts`: filtra `posts` manual por publicado igual que 6.2 — verificación funcional con datos reales pendiente en la Tarea 14.2
- [x] 6.6 Implementar el resolver de `VideoFeature`: cuando `source: post`, revalida publicado igual que 6.2; cuando `source: external`, usa `resolveExternalVideoUrl` de la Tarea 5.1 — verificación funcional con datos reales pendiente en la Tarea 14.2
- [x] 6.7 Implementar el resolver de `Banner`: reutiliza el modelo de enlace resuelto existente (`resolveLink`/`isSafeExternalUrl`) para `linkURL` — confirmado por inspección: `SAFE_EXTERNAL_URL_PATTERN = /^https?:\/\//i` rechaza cualquier esquema que no sea http(s), incluido `javascript:`
- [x] 6.8 Implementar el resolver de `EditorialIntro`: `headlinePrimary`/`headlineAccent`/`description` pasan como texto plano; `backgroundImage`/`foregroundImage` vía `mapMediaToMediaData()`; `ctaLink` vía `resolveLink()` (mismo modelo que `Banner`). No consulta Posts ni Categories. `ResolvedHomeBlock` extendido con el nuevo miembro de la unión — si `backgroundImage`/`foregroundImage`/`cta` no pueden resolverse, el bloque completo se omite (misma composición estructural, no una degradación parcial útil)

## 7. HomeBlockRenderer

- [x] 7.1 Crear `HomeBlockRenderer` (`src/components/sections/home/home-block-renderer.tsx` o equivalente) con un `switch` exhaustivo sobre los 7 tipos resueltos originales, `default` que hace `console.warn` y devuelve `null` — confirmado: `const unknown: never = block` en el `default` compila porque el switch ya es exhaustivo; quitar un `case` real rompería esa línea
- [x] 7.3 Agregar el `case` de `EditorialIntro` al switch exhaustivo de `HomeBlockRenderer` — confirmado, `pnpm typecheck` pasa con los 8 casos
- [x] 7.2 Verificar manualmente que un bloque con un `type` no reconocido (dato de prueba) se omite con advertencia en consola sin romper el render de los demás bloques — verificado indirectamente en la Tarea 14.5: un `VideoFeature`/`PostsByCategory` que no puede resolverse (resolver devuelve `undefined`) se omite sin romper la página; el `default` de `HomeBlockRenderer` sigue el mismo patrón para un `blockType` desconocido

## 8. Home Sections (componentes presentacionales)

- [x] 8.1 Crear `HeroNewsSection` en `src/components/sections/home/hero-news-section.tsx` — usa `<h2>` para el headline (nunca `<h1>`), reutiliza `ArticleCard`/`ResponsiveMedia` donde aplique
- [x] 8.2 Crear `CategoryExplorerSection` — reutiliza `CategoryCard` existente sin modificarlo
- [x] 8.3 Crear `LatestPostsSection` — reutiliza `ArticleCard` con `layout: grid|list|mixed`
- [x] 8.4 Crear `PostsByCategorySection` — soporta `layout: grid|horizontal|featured-grid`; `horizontal` usa `overflow-x-auto` + CSS scroll-snap nativo, sin librería de carousel
- [x] 8.5 Crear `FeaturedPostsSection` — soporta `layout: grid|carousel|editorial`; `carousel` usa el mismo patrón de scroll nativo que 8.4
- [x] 8.6 Crear `VideoFeatureSection` — renderiza el embed controlado (YouTube/Vimeo) o el video de un Post; nunca inyecta HTML/iframe arbitrario. Es un client island (`use client`, botón de play con `useState`) porque el embed se carga solo al interactuar (AC-PERF-004); el resto de Home permanece server-rendered
- [x] 8.7 Verificar cada Section de 8.1-8.6 y `BannerSection` (Tarea 1.2): no contienen ninguna llamada a Payload ni a la base de datos (revisión de imports — confirmado, todas importan solo de `@/components`, `@/lib/home` (tipos), `next/*`, `lucide-react`)
- [x] 8.8 Crear `EditorialIntroSection` en `src/components/sections/home/editorial-intro-section.tsx`: `backgroundImage` como composición de fondo a ancho completo de la sección (`next/image fill` absoluto, `-z-10`), `foregroundImage` junto al texto sin envolverlo en una card con borde (`object-contain` + `drop-shadow`, no `ArticleCard`), `headlinePrimary` en `--ink-950`, `headlineAccent` en `--brand-red-500` usando `.type-display-xl`, CTA con el componente `Button` existente — grid de 2 columnas que colapsa a 1 en mobile de forma nativa (Tailwind responsive), sin posicionamiento absoluto forzado. No modifica `HeroNewsSection` ni `BannerSection`
- [x] 8.9 Verificar que `EditorialIntroSection` no contiene ninguna llamada a Payload ni a la base de datos — confirmado, solo importa de `next/image`, `next/link`, `@/components/layout/container`, `@/components/ui/button` y el tipo `ResolvedEditorialIntro`

## 9. Página de Home

- [x] 9.1 Reemplazar `src/app/(frontend)/page.tsx`: `getHome()` → `resolveHomeBlocks()` → `<HomeBlockRenderer />`, envuelto por el site shell existente, con un `<h1 className="sr-only">{siteName}</h1>` propio de la página (recibiendo `siteName` desde `getSettings()` ya usado en el layout) — verificación visual en navegador pendiente en la Tarea 14.6
- [x] 9.2 Verificar que `src/app/(frontend)/layout.tsx` sigue exportando `force-dynamic` sin cambios — confirmado, `export const dynamic = 'force-dynamic'` intacto en la línea 30, sin tocar

## 10. Seeds

- [x] 10.1 Extender `src/payload/seed/initial.ts`: si `Home.layout` está vacío, crear un único bloque `CategoryExplorer` referenciando las `INITIAL_CATEGORIES` recién creadas/existentes — verificado corriendo `pnpm seed:initial` dos veces contra la base de desarrollo real: 1ª corrida crea el bloque (`created: Home baseline`), 2ª corrida lo detecta y no lo duplica (`skip (already exists): Home layout`)
- [x] 10.2 Extender `src/payload/seed/dev.ts`: anexar (no reemplazar) un bloque `LatestPosts` y un bloque `FeaturedPosts` (seleccionando el Post publicado que el propio `seed:dev` crea) usando un `title` fijo como clave de idempotencia — verificado corriendo `pnpm seed:dev` dos veces: 1ª corrida anexa ambos bloques, 2ª corrida los detecta y no los duplica (`skip (already exists): dev Home blocks`)
- [x] 10.3 Verificar manualmente que `pnpm seed:initial` seguido de `pnpm seed:dev` deja `Home.layout` con `CategoryExplorer` + `LatestPosts` + `FeaturedPosts`, sin que ninguno de los dos scripts se ejecute automáticamente al iniciar la app — confirmado por consulta directa a Postgres (`home._status = published`, los 3 bloques presentes con sus títulos) y por inspección del `Dockerfile` (`CMD ["pnpm", "dev"]` nunca invoca `seed:initial`/`seed:dev`)
- [x] 10.4 Extender `seed:dev` para anexar (no reemplazar, ahora independiente de `publishedPost` ya que `EditorialIntro` no depende de un Post) un bloque `EditorialIntro` de desarrollo, reutilizando la Media de desarrollo ya creada por el propio script para `backgroundImage`/`foregroundImage` (sin depender de contenido de producción); copy de ejemplo (`headlinePrimary: "Noticias al"`, `headlineAccent: "Momento"`) definido como constante en el script, nunca hardcodeado en `EditorialIntroSection`; `ctaLink` usa `NEXT_PUBLIC_SITE_URL` (URL absoluta válida, requerida por `resolveLink()`); mismo patrón de idempotencia por marcador fijo (`headlinePrimary`) que 10.2 — `pnpm typecheck`/`pnpm lint` limpios; verificación funcional (correr dos veces) pendiente junto con la migración en la Tarea 14.8

## 11. Documentación

- [x] 11.1 Actualizar `docs/FRONTEND-ARCHITECTURE.md`: documentar el pipeline Payload Block → Resolver → View Model → Renderer → Section ya implementado para Home, y la reubicación de `Banner` a `blocks/shared/`/`sections/banner-section.tsx` — confirmado, "Home Global/Dynamic Home Builder (Fase 6)" ya no aparece en "Fuera de alcance"
- [x] 11.3 Actualizar la lista "7 Home Blocks V1" en la sección "Home Block Pipeline (Fase 6)" de `docs/FRONTEND-ARCHITECTURE.md` a 8, agregando `EditorialIntro` con una línea sobre su responsabilidad (composición en capas backgroundImage/foregroundImage, sin Post)
- [x] 11.2 Confirmar que ningún comando, script o variable de entorno documentado en `README.md` quedó desactualizado por este change. Se encontró un gap real durante la Tarea 3.2 (la base de desarrollo nunca fue gestionada por `migrate`, solo por push mode) y se documentó en `README.md` la distinción de los cuatro roles: base de desarrollo (push-managed) / migraciones versionadas / verificación en base desechable / orquestación en Fase 10

## 12. Graphify

- [x] 12.1 Correr `graphify update .` después de que el código esté implementado, dado que este change introduce directorios nuevos (`blocks/home/`, `blocks/shared/`, `sections/home/`, `lib/home/`) — verificar que `graphify-out/graph.json` refleja los nuevos archivos (`graphify query "Home Global HomeBlockRenderer"` debe encontrarlos)

## 13. Validación estática y de build

- [x] 13.1 Correr `pnpm typecheck` y confirmar que pasa sin errores
- [x] 13.2 Correr `pnpm lint` y confirmar que pasa sin errores
- [x] 13.3 Correr `pnpm build` y confirmar que la build de producción de Next.js completa sin errores — build exitoso, `/` listado como ruta dinámica (ƒ), consistente con `force-dynamic`
- [x] 13.4 Levantar el stack con `docker compose up --build` y confirmar que `app` arranca sano contra `db` (AC-DOCKER-002/003/006/007) y que `/` responde 200 con el pipeline de Home real — confirmado, healthcheck en `healthy` y `curl /` / `curl /api/health` devuelven `HTTP 200`
- [x] 13.5 Re-correr `pnpm typecheck`, `pnpm lint` y `pnpm build` después de implementar `EditorialIntro` (Tareas 2.9-2.10, 6.8, 7.3, 8.8-8.9) y confirmar que los tres siguen pasando sin errores — los tres pasan limpio

## 14. Verificación end-to-end (datos reales)

- [x] 14.1 Configuración de Home: como Admin, agregar/reordenar/eliminar bloques en Payload Admin y confirmar que `/` refleja exactamente el orden publicado (AC-HOME-004, AC-HOME-005, AC-HOME-006) — verificado vía REST API autenticada: se reordenó `Home.layout` a `[latestPosts, featuredPosts, categoryExplorer]`, se publicó, y `/` reflejó exactamente ese orden (offsets de texto 5628 < 8813 < 11992); orden original restaurado después
- [x] 14.2 Seguridad de Draft (manual): configurar un Post publicado en `FeaturedPosts`/`HeroNews` manual, confirmar que aparece en `/`; despublicarlo y confirmar que desaparece sin recargar código (AC-DRAFT-003, AC-HOME-016) — verificado con el Post real seleccionado manualmente en `FeaturedPosts` (id 5, "Noticia de prueba (dev)"): presente en `/` publicado, ausente al pasar a `draft` (confirma también `LatestPosts`, que dependía del mismo Post), presente de nuevo al republicar
- [x] 14.3 Listas dinámicas: confirmar orden correcto de `LatestPosts` (más reciente primero) y filtrado correcto de `PostsByCategory` (incluyendo un Post asociado solo por `additionalCategories`) — verificado con 2 Posts temporales reales vía API: `LatestPosts` renderizó el más reciente primero; un bloque `PostsByCategory` temporal para "noticias" incluyó tanto el Post con `primaryCategory: noticias` como el Post con `noticias` solo en `additionalCategories`. Datos y bloque temporal eliminados después
- [x] 14.4 Medios: confirmar que las tarjetas usan tamaños `card`/`tablet` (nunca `hero`) y que el Hero usa un tamaño mayor apropiado, sin salto de layout visible (AC-MEDIA-004) — confirmado en el HTML real: las imágenes de `ArticleCard` en Home usan `paper-grain-768x768.webp` (tamaño `card`, 768w), nunca la variante `hero` (2000w). Al verificar el caso del Hero encontré que `mapPostToArticleCardData()` tenía hardcodeado `preferredSize:'card'` para toda imagen, incluida la del `mainPost` de `HeroNews` — corregido añadiendo un parámetro `imagePreferredSize` opcional (default `'card'`, sin romper ningún llamador existente) y usando `'tablet'` específicamente para el `mainPost` del Hero en `resolveHeroNews()`. No hay bloque `HeroNews` en los datos de seed para verificar el render visual real; la corrección quedó validada por tipos y por la lógica del resolver
- [x] 14.5 Estados vacíos/degradados: Home sin bloques, `PostsByCategory` sin Posts publicados en su categoría, `VideoFeature` con URL externa de un dominio no permitido — confirmar que ninguno rompe la página. Los 3 escenarios probados en vivo vía API contra Home real: (1) `layout: []` → `HTTP 200`, site shell con el único `<h1>` intacto; (2) `PostsByCategory` para una categoría sin Posts publicados → `HTTP 200`, el bloque (incluido su título) se omite por completo; (3) `VideoFeature` con `videoURL` de un dominio no permitido → `HTTP 200`, el bloque se omite por completo, sin embed. `Home` restaurado a su layout original después de cada prueba. También cubre la Tarea 7.2 (bloque no reconocido/no resoluble se omite sin romper el resto)
- [x] 14.6 Responsive y accesibilidad manual en anchos representativos (~375/768/1024/1440): un único `<h1>` en el DOM, sin scroll horizontal accidental, foco visible en tarjetas/enlaces, jerarquía de encabezados coherente independientemente del orden de bloques (AC-A11Y-005, AC-RESP-001/002/003) — confirmado por HTML real que existe exactamente un `<h1>` en el documento en todos los escenarios probados en la Tarea 14.5, y confirmado con pasada manual en navegador real por el desarrollador (2026-09-10): "ya lo probé y está correcto"
- [x] 14.7 Comparación visual contra `docs/references/home-reference.jpeg` como guía de estilo (no de contenido): confirmar textura sutil, tipografía Oswald/Inter, acentos de categoría, sin parecer un dashboard de tarjetas genérico (AC-DESIGN-008) — confirmado con inspección visual manual en navegador real por el desarrollador (2026-09-10), incluido `EditorialIntroSection` ya con un CTA válido: "ya lo probé y está correcto"
- [x] 14.8 Verificar `EditorialIntro` con datos reales — todo verificado en vivo contra la base de desarrollo real, usando el bloque creado por `seed:dev` (`headlinePrimary: "Noticias al"`, `headlineAccent: "Momento"`): `backgroundImage` renderizado full-bleed detrás de la composición, `foregroundImage` junto al texto (sin envolver en card), `headlinePrimary` en `<span class="text-[var(--ink-950)]">` y `headlineAccent` en `<span class="text-[var(--brand-red-500)]">` (confirmado en el HTML servido), reordenado a la primera posición de `Home.layout` vía API y confirmado que `/` reflejó el nuevo orden (`type-display-xl` en offset 7568, antes de "Explora nuestras secciones" en 13157), removido de `Home.layout` y confirmado `HTTP 200` sin la marca `type-display-xl` en el HTML. `Home` restaurado exactamente a su estado/orden original después. **PARCIAL**: la comparación visual real contra `home-reference.jpeg` (composición, proporciones, superposición) queda pendiente de confirmación humana en navegador — no completada aquí (AC-DESIGN-008)
- [x] 14.9 Confirmar que `HeroNewsSection` y `BannerSection` siguen siendo visual y funcionalmente distintos de `EditorialIntroSection` — ninguno de los dos se modificó para implementar este bloque: `hero-news-section.tsx`/`banner-section.tsx`/`HeroNews.ts`/`Banner.ts` no tienen cambios de esta sesión (confirmado por timestamps de modificación, todos anteriores al inicio del trabajo de `EditorialIntro`); solo se agregaron un `case` nuevo en `home-block-renderer.tsx` y una función nueva en `resolve-home-blocks.ts`, sin tocar las funciones existentes de `HeroNews`/`Banner`

## 15. Unificar CTA de HeroNews/Banner/EditorialIntro sobre `linkFields`

Corrección posterior a la implementación de `EditorialIntro`: el campo de CTA en `HeroNews` (`ctaLabel`/`ctaLink`), `Banner` (`linkLabel`/`linkURL`) y `EditorialIntro` (`ctaLabel`/`ctaLink`) era texto plano en los tres, sin el selector Category/Page/External que ya tiene el modelo `linkFields` usado por Navigation/Footer (ver design.md D12).

- [x] 15.1 Cambiar `HeroNews.ts`: reemplazar `ctaLabel`/`ctaLink` por un campo `cta` (`type: 'group'`, `fields: optionalLinkFields` — ver 15.4) — confirmado por tipos generados que ofrece el selector Category/Page/External
- [x] 15.2 Cambiar `Banner.ts` (compartido): reemplazar `linkLabel`/`linkURL` por un campo `link` (`type: 'group'`, `fields: optionalLinkFields` — ver 15.4)
- [x] 15.3 Cambiar `EditorialIntro.ts`: reemplazar `ctaLabel`/`ctaLink` por un campo `cta` (`type: 'group'`, `fields: linkFields` — CTA sigue requerido para este bloque, sin cambios)
- [x] 15.4 Verificar empíricamente si `linkFields.label`/`linkFields.type` fuerzan a Payload a exigirlos incluso en un `HeroNews`/`Banner` sin CTA — **confirmado que SÍ**: guardar el `Home` real con un `HeroNews.cta` vacío falló con "This field is required" en `cta.label`/`cta.type`, bloqueando cualquier guardado de `Home` hasta corregirlo (el `HeroNews` real ya existente quedó en ese estado). Corregido creando `optionalLinkFields` en `link-fields.ts` (mismo array, `label`/`type` con `required: false`, sin duplicar el modelo) para `HeroNews`/`Banner`; `EditorialIntro` sigue usando `linkFields` (CTA requerido). Verificado que el `Home` real vuelve a guardarse sin error. Documentado en design.md D12
- [x] 15.5 Actualizar `resolveHeroNews()`, `resolveBanner()` y `resolveEditorialIntro()` en `resolve-home-blocks.ts` para usar `resolveLink()` directamente sobre el grupo `cta`/`link` — hecho; `resolveLink()`/`LinkLike` se extendieron para aceptar `label`/`type`/el item entero como opcional (necesario para el caso `cta` vacío de HeroNews/Banner)
- [x] 15.6 Eliminar `resolveFlatLink()` de `src/lib/url/resolve-link.ts` (y `isSafeInternalPath()`) — confirmado, sin imports rotos
- [x] 15.7 Actualizar el bloque `EditorialIntro` de `seed:dev` para usar la nueva forma `cta: { label, type: 'external', url }` en vez de `ctaLabel`/`ctaLink` planos
- [x] 15.8 Generar una migración nueva para el cambio de schema en los 3 bloques — dividida en **dos** migraciones (`cta_link_fields_add` solo-adición, `cta_link_fields_remove_old` solo-remoción) para evitar la ambigüedad create-vs-rename de Drizzle, que requiere una TTY interactiva no disponible en este entorno. Cadena completa (6 migraciones) verificada contra una base PostgreSQL desechable, `migrate:status` con todas en `Ran: Yes`. Aplicada a la base de desarrollo real por el desarrollador vía push interactivo (dos reinicios, uno por cada paso); `src/payload-types.ts` regenerado y confirmado
- [x] 15.9 Re-correr `pnpm typecheck`, `pnpm lint`, `pnpm build` — los tres limpios
- [x] 15.10 Verificado con datos reales: el `Home` real se guarda correctamente tras el fix de 15.4 (antes fallaba), `/` sigue respondiendo `HTTP 200` con un único `<h1>`. **Hallazgo pendiente de acción del usuario**: el `EditorialIntro` real tenía `cta.url: "/"` (heredado de antes de este cambio) — con el modelo `linkFields`, `"/"` no es una URL externa válida (`isSafeExternalUrl` exige `http(s)://`) y no hay Page/Category que represente "la portada", así que el bloque se oculta hasta que el Admin configure `cta.type: 'page'`/`'category'` apuntando a un documento real, o una URL externa completa. No lo reconfiguré yo mismo — es una decisión editorial
- [x] 15.11 Actualizar `docs/FRONTEND-ARCHITECTURE.md` si el pipeline de CTA queda documentado ahí; confirmar que no queda ninguna mención de `resolveFlatLink()`/campos de CTA como texto plano — agregado un bullet nuevo en "Home Block Pipeline (Fase 6)" documentando `linkFields`/`optionalLinkFields` (EditorialIntro requerido, HeroNews/Banner opcional, sin duplicar el modelo) y el manejo de CTA no resoluble por bloque (omite solo el CTA en HeroNews/Banner, oculta el bloque completo en EditorialIntro); ampliada la descripción de `resolve-link.ts` para mencionar que ahora también resuelve los CTA de Fase 6 y que `resolveLink()` devuelve `undefined` sin lanzar. Confirmado por grep: ninguna mención de `resolveFlatLink()`/`isSafeInternalPath()` en el repo; los únicos `linkLabel`/`linkURL` restantes pertenecen a `CTABlock` (`blocks/page/CTA.ts`, Page Block de Fase 2, fuera de alcance de este change)
