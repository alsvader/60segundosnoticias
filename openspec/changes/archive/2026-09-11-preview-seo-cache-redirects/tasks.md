## 1. Origen de sitio canónico

- [x] 1.1 Agregar `getSiteOrigin()`/`getAbsoluteUrl(path)` a `src/lib/url/canonical.ts`, leyendo `env.NEXT_PUBLIC_SITE_URL` (nunca `process.env` directo) — verificar con `pnpm typecheck` y una prueba manual con la variable ausente en un build de producción (debe fallar explícitamente, no caer a `localhost`)
- [x] 1.2 Reemplazar la construcción ad hoc de `[category]/[post]/page.tsx:62-63` (`process.env.NEXT_PUBLIC_SITE_URL ?? 'http://localhost:3000'`) por `getAbsoluteUrl()` — verificar que `ShareActions` sigue mostrando la misma URL que antes en desarrollo

## 2. Esquema de cache y helper de invalidación

- [x] 2.1 Confirmar contra Next.js 16.3.3 instalado (sin `cacheComponents`) el comportamiento exacto de `unstable_cache` + Draft Mode bypass — documentar el resultado en un comentario del módulo antes de envolver el DAL
- [x] 2.2 Crear `src/lib/cache/tags.ts` con los tags conceptuales (`post:{id}`, `category:{id}`, `posts`, `home`, `page:{id}`, `navigation`, `footer`, `settings`, `sitemap`, `llms`) — verificar con `pnpm typecheck`
- [x] 2.3 Envolver con `unstable_cache` cada función pública del DAL (`getPostBySlug`, `getLatestPosts`, `getPostsByCategory`, `getRelatedPosts`, `getFeaturedPosts`, `getNewestPostPerCategory`, `getCategoryBySlug`, `getCategoriesForNavigation`, `getAllCategories`, `getPageBySlug`, `getHome`, `getNavigation`, `getFooter`, `getSettings`, `getArticleSidebar`) con sus tags correspondientes — verificado con `pnpm typecheck`/`pnpm lint` limpios; `getPostBySlug`/`getCategoryBySlug`/`getPageBySlug` se tagean por `slug` (el `id` no se conoce antes de resolver la consulta), no por `id` — ver comentarios en cada archivo. Hallazgo durante implementación: el Global `ArticleSidebar` (Fase 7) no estaba en la matriz de invalidación original — se agregó tag `article-sidebar` (`CACHE_TAGS.articleSidebar`) y `invalidateArticleSidebar()`, mismo tratamiento que Navigation/Footer (ver tarea 3.6 nueva)
- [x] 2.4 Crear `src/lib/cache/invalidate.ts` (`invalidatePost()`, `invalidateCategory()`, `invalidatePage()`, `invalidateHome()`, `invalidateShell()`) según la matriz de `specs/cache-revalidation/spec.md` — cada función captura y registra errores de `revalidateTag()` sin relanzarlos (AC-ERR-003; nota: `invalidateShell()` se implementó como tres funciones específicas `invalidateNavigation()`/`invalidateFooter()`/`invalidateSettings()`, mismo efecto, ver `src/lib/cache/invalidate.ts`)

## 3. Hooks de invalidación por Collection/Global

- [x] 3.1 Agregar hook `afterChange`/`afterDelete` a `Posts.ts` que invoque `invalidatePost()` con el conjunto de tags correspondiente (post/categorías/posts/home condicional) — branching draft-vs-published (mismo patrón de `resultingStatus` que `publish-validation.ts`) — código en `src/payload/hooks/posts/cache-invalidation.ts`, wired en `Posts.ts`; verificación en vivo (publish/unpublish real) pendiente en la sección 16
- [x] 3.2 Agregar hook a `Categories.ts` que invoque `invalidateCategory()` (category/navigation condicional/Posts afectados si cambia el slug) (AC-CACHE-003) — código en `src/payload/hooks/categories/cache-invalidation.ts`; verificación en vivo pendiente en la sección 16
- [x] 3.3 Agregar hook a `Pages.ts` que invoque `invalidatePage()` (page/navigation-footer condicional) — código en `src/payload/hooks/pages/cache-invalidation.ts`; verificación en vivo pendiente en la sección 16
- [x] 3.4 Agregar hook a `Home.ts` que invoque `invalidateHome()` (solo tag `home`, AC-CACHE-004) — código en `src/payload/hooks/home/cache-invalidation.ts`; verificación en vivo pendiente en la sección 16
- [x] 3.5 Agregar hooks a `Navigation.ts`/`Footer.ts`/`SiteSettings.ts` que invoquen su invalidación específica (AC-CACHE-005) — código en `src/payload/hooks/shell/cache-invalidation.ts`; nota: `SiteSettings` solo revalida `llms` cuando cambian `branding`/`seo` (ver comentario en el módulo — `sitemap.xml` no lee ningún campo de SiteSettings, Decisión 1); verificación en vivo pendiente en la sección 16
- [x] 3.6 (hallazgo durante 2.3) Agregar hook a `src/payload/globals/ArticleSidebar.ts` que invoque `invalidateArticleSidebar()` — Global de Fase 7 no cubierto por la matriz de invalidación original — código en `src/payload/hooks/shell/cache-invalidation.ts`; verificación en vivo pendiente en la sección 16

## 4. Remoción verificada de force-dynamic

- [x] 4.1 Con 2 y 3 completos y verificados, remover `export const dynamic = 'force-dynamic'` de `src/app/(frontend)/layout.tsx` — verificado en **modo desarrollo** (`next dev` vía Docker): PATCH autenticado a `/api/posts/32` (excerpt) via REST, GET inmediato subsecuente a `/entretenimiento/festival-musica-independiente` refleja el cambio al instante (sin stale-serve), sin errores en logs; Home/Category/Article siguen respondiendo 200 tras el hot-reload. **Nota importante**: `next dev` renderiza fresco en cada solicitud independientemente de static/dynamic, así que esto NO prueba el comportamiento de ISR/static generation + `revalidateTag` en un build de producción real (`next build && next start`) — eso se verifica específicamente en 15.3/15.4/16.5, antes de considerar esta remoción completamente validada. Navigation/Footer/SiteSettings (requieren un usuario Admin real, no disponible en este entorno) quedan para verificación conjunta con el usuario en la sección 16 (AC-NAV-004, AC-FOOT-001/002)

## 5. SEO metadata

- [x] 5.1 Crear `src/lib/seo/` con la función de resolución de la cadena de fallback (SEO del documento → contenido → `SiteSettings.seo`) — `src/lib/seo/metadata.ts` (`buildMetadata`/`buildArticleMetadata`); verificado en vivo (ver 5.2-5.4)
- [x] 5.2 Implementar `generateMetadata` en `src/app/(frontend)/page.tsx` (Home) — verificado en vivo contra Docker dev: `<title>60 Segundos Noticias</title>`, canonical `http://localhost:3000` (AC-SEO-005)
- [x] 5.3 Implementar `generateMetadata` en `src/app/(frontend)/[category]/page.tsx` para Category, con fallback `"{name} | 60 Segundos"` — verificado en vivo: `<title>Entretenimiento | 60 Segundos</title>`, canonical correcto, `og:type=website` (AC-SEO-003). Sin SEO propio configurado en los datos de seed actuales — el fallback es el único camino ejercitado en vivo; el override queda cubierto por el mismo `buildMetadata()` que ya se probó para Article
- [x] 5.4 Implementar `generateMetadata` en `src/app/(frontend)/[category]/[post]/page.tsx` para Article (dedupe con `cache()` sobre el argumento primitivo `slug`, no un objeto literal) — verificado en vivo: título/descripción/canonical/OG completos, `og:type=article`, imagen OG absoluta (`/api/media/...` normalizada a `http://localhost:3000/...` vía `getAbsoluteUrl`), canonical idéntico a la URL que `ShareActions` ya mostraba (AC-SEO-001, AC-SEO-002)
- [x] 5.5 Implementar `generateMetadata` para Page genérica (mismo archivo `[category]/page.tsx`), con fallback a `Hero.description`/`Hero.image` cuando el primer block es un Hero (AC-SEO-004) — verificado en vivo con una Page real de prueba (con credenciales de Admin): `<title>Pagina De Prueba Phase8</title>`, `<meta name="description" content="Descripcion de prueba para metadata."/>` desde el Hero block, canonical correcto (AC-SEO-004)
- [x] 5.6 Confirmar por auditoría que ninguna metadata usa una URL absoluta construida fuera de `getAbsoluteUrl()` — `grep` confirma que el único `new URL(...)` fuera de `canonical.ts` es `metadataBase: new URL(getSiteOrigin())` en el layout raíz, que sigue usando la función centralizada

## 6. Structured data (JSON-LD)

- [x] 6.1 Crear el helper de serialización segura de JSON-LD en `src/lib/seo/json-ld.tsx` (`JsonLd`, escape de `<` antes de `dangerouslySetInnerHTML`) — verificado en vivo: los 5 bloques JSON-LD renderizados (Home x2, Category, Article x2) son JSON válido en el HTML servido
- [x] 6.2 Agregar JSON-LD `Organization`/`WebSite` a Home, desde `resolveOrganizationInfo(settings)` (`SiteSettings.organization`, con fallback a `branding.siteName`) — verificado en vivo; hallazgo: `SiteSettings` está vacío en los datos de desarrollo actuales (`{}` en todos sus grupos), así que el valor observado es el mismo fallback literal `'60 Segundos Noticias'` que ya usaba `HomePage` antes de esta change, no un valor nuevo hardcodeado por mí (AC-SEO-008)
- [x] 6.3 Agregar JSON-LD `NewsArticle` + `BreadcrumbList` a Article, con autor limitado al `AuthorSummary` existente (`displayName`/`slug`/`avatar`/`bio`, nunca `email`/`role`/`active`) — verificado en vivo contra el Article real usado en 5.4: `author` en el JSON-LD solo expone `{"@type":"Person","name":"Writer Uno (dev)"}` (AC-SEO-006, AC-SEO-007)
- [x] 6.4 Agregar JSON-LD `BreadcrumbList` a Category — verificado en vivo: coincide exactamente con el breadcrumb visual ya renderizado (Inicio → Entretenimiento)

## 7. sitemap.xml / robots.txt

- [x] 7.1 Crear `src/app/sitemap.ts` (`MetadataRoute.Sitemap`) — nota: se ubicó en la raíz de `app/`, no dentro de `(frontend)/` como se había esbozado (los route groups no afectan la URL resultante, pero `sitemap.ts`/`robots.ts` son convenciones de archivo globales, no específicas del frontend). Home + Categories + Pages publicadas + Posts publicados (URL con `primaryCategory`, nunca `additionalCategories`/`Redirects.from`) — nuevas proyecciones estrechas `getPublishedPostsForSitemap()`/`getPublishedPagesForSitemap()`. Verificado en vivo: `GET /sitemap.xml` devuelve las 5 Categories y los 15 Posts publicados reales del seed con URLs y `lastmod` correctos (AC-SEO-009, AC-SEO-010, AC-SEO-011)
- [x] 7.2 Crear `src/app/robots.ts` (`MetadataRoute.Robots`): producción permite indexación y referencia el sitemap; no-producción bloquea vía `NODE_ENV` (entorno ya existente, sin variable nueva) — sin reglas por user-agent de IA — verificado en vivo en modo desarrollo: `GET /robots.txt` → `User-Agent: *` / `Disallow: /`; el caso de producción se verifica en la sección 15/16 contra un build real (AC-SEO-012, AC-SEO-013)
- [x] 7.3 Envolver el ensamblado de datos de sitemap con el tag `sitemap` (`unstable_cache`) — hecho en `src/app/sitemap.ts` (una sola `unstable_cache` envolviendo Categories+Posts+Pages); cada hook de invalidación de Post/Category/Page ya revalida `sitemap` explícitamente (sección 3) — verificación end-to-end (publicar → sitemap actualizado) pendiente en la sección 16

## 8. Redirects — lookup en runtime

- [x] 8.1 Crear `findActiveRedirectByPath(path)` en `src/lib/data/redirects.ts` (`overrideAccess: true` acotado, `select: { to, statusCode }`, nunca el documento completo) — confirmado por inspección de imports que solo `resolveRootSlug()` y el Article page lo invocan, ningún componente
- [x] 8.2 Confirmar contra la API de la versión instalada de Next.js el mecanismo exacto para emitir el `statusCode` almacenado (301/302) al redirigir — documentado en `src/lib/redirects/apply-redirect.ts`: Server Components solo pueden emitir 307 (`redirect()`)/308 (`permanentRedirect()`), nunca un 301/302 literal; `301`→`permanentRedirect` (308), `302`→`redirect` (307) — **hallazgo crítico durante implementación**: `src/lib/cache/references.ts`, `src/lib/cache/invalidate.ts` y `src/lib/url/canonical.ts` tenían `import 'server-only'`, lo que rompía `payload run`/`migrate`/`generate:types` (cargan `payload.config.ts` fuera del bundler de Next.js) en cuanto un hook de Payload los importaba transitivamente — confirmado en vivo (`payload run` fallaba con "This module cannot be imported from a Client Component"). Se removió el guard de los tres módulos y `getSiteOrigin()` pasó a leer `process.env` directo (mismo patrón ya usado por `src/lib/env/payload.ts`) — verificado que `payload run` vuelve a funcionar
- [x] 8.3 Integrar el lookup en el miss path de `resolveRootSlug()` (antes de `notFound()`, nuevo caso `'redirect'` en `ResolvedRootSlug`) — verificado en vivo: Redirect real creado vía Local API (`/old-test-path-phase8` → `/entretenimiento/festival-musica-independiente`, `301`) responde `308` con la URL correcta; limpiado después
- [x] 8.4 Integrar el lookup en el miss path de la búsqueda de Post por slug en `[category]/[post]/page.tsx` — verificado en vivo: Redirect real (`/deportes/articulo-viejo-phase8-test` → artículo real, `302`) responde `307` con la URL correcta; limpiado después
- [x] 8.5 Verificar que una ruta actualmente válida nunca es interceptada por un Redirect histórico — verificado en vivo: Redirect de prueba con `from: '/entretenimiento'` (Category real y vigente) creado vía Local API; `GET /entretenimiento` siguió respondiendo `200` con el contenido real de la Category, nunca el redirect; limpiado después

## 9. Redirects — generación automática

- [x] 9.1 Confirmar contra Payload 3.87.1 instalado la forma exacta de acceder a `previousDoc` dentro de un hook `afterChange` — confirmado por inspección de tipos (`node_modules/payload/dist/collections/config/types.d.ts`): `{ collection, context, data, doc, operation, overrideAccess, previousDoc, req }`; `previousDoc.primaryCategory` llega como id crudo (no poblado), documentado en `src/payload/hooks/posts/redirect-lifecycle.ts`
- [x] 9.2 Crear el helper compartido de creación/aplanado de redirect (`src/lib/redirects/create-historical-redirect.ts`): aplanado hacia adelante y hacia atrás, upsert por `from`, rechazo de auto-redirect — **bug real encontrado y corregido mediante prueba en vivo**: el primer intento, al detectar un ciclo (`A→B` ya existente + intento de crear `B→A`), simplemente abortaba sin hacer nada, dejando `A→B` activo e incorrecto (`A` ya volvía a ser una ruta vigente) — reproducido en vivo con un rename→rename-back real de un Post vía REST API (la URL original quedó devolviendo 308 en vez de 200). Corregido: ahora desactiva (`active: false`, nunca elimina) los redirects recorridos que forman el ciclo y crea el nuevo redirect con el destino solicitado originalmente — re-verificado en vivo con el mismo escenario: `A` responde 200, `B` responde 308 hacia `A`
- [x] 9.3 Agregar hook a `Posts.ts` (`createPostRedirect`) que invoque el helper en cambio de `slug`/`primaryCategory` de un Post publicado, con salto único en cambio simultáneo — verificado en vivo end-to-end vía REST API real (rename → redirect 308 automático; rename de vuelta → redirect anterior desactivado + nuevo redirect inverso creado) (AC-REDIR-001, AC-REDIR-003)
- [x] 9.4 Agregar hook a `Categories.ts` (`createCategoryRedirect`) que invoque el helper en cambio de `slug`, cascadeando un redirect por cada Post publicado con esa `primaryCategory` — verificado en vivo (Local API, categoría real "Entretenimiento" con 6 Posts publicados): se crearon correctamente los 7 redirects esperados (1 de categoría + 6 de Posts) y se revirtieron limpiamente. Nota: la invalidación de cache específicamente no se pudo verificar en esta prueba porque se disparó vía `payload run` (fuera del contexto de request de Next.js, donde `revalidateTag` genuinamente no puede ejecutar — confirmado por el propio error capturado y registrado por `safeRevalidateTag`, sin romper la mutación, tal como exige AC-ERR-003); la invalidación de cache para Categories ya se probó correcta para Posts vía REST API real, mismo mecanismo — verificación específica con una mutación real de Categories vía Admin queda para la sección 16 (AC-REDIR-002)
- [x] 9.5 Agregar hook a `Pages.ts` (`createPageRedirect`) que invoque el helper en cambio de `slug` de una Page publicada — verificado en vivo con una Page real de prueba (con credenciales de Admin): cambiar su slug crea el redirect (301 almacenado → 308 servido), la URL antigua redirige a la nueva, la nueva responde 200
- [x] 9.6 Confirmar por auditoría que eliminar un Post/Category/Page no crea ningún redirect — confirmado por inspección: ningún hook `afterDelete` (`invalidatePostCacheOnDelete`, `invalidateCategoryCacheOnDelete`, `invalidatePageCacheOnDelete`) invoca `createHistoricalRedirect`, solo los hooks `afterChange` lo hacen (AC-REDIR-005)

## 10. Preview / Draft Mode

- [x] 10.1 Crear la función de resolución de documento de preview (`src/lib/preview/resolve-preview-document.ts`, explícitamente nombrada, fuera de `public-query.ts`) usando `payload.auth({headers})` + `overrideAccess: false` + el `access.read` existente de Posts/Pages — verificado en vivo end-to-end con credenciales de Admin reales y con sesiones de cookies reales (login → Preview → Exit) para `writer.uno` (autor), `writer.dos` (no autor) y Admin: el autor y el Admin ven el contenido en Draft real de un Post; `writer.dos` ve la versión publicada (nunca la revisión en Draft) tanto en `/api/preview` como en el render real de la página (AC-PREVIEW-001/002/005)
- [x] 10.2 Crear `src/app/api/preview/route.ts`: valida `PREVIEW_SECRET`, resuelve el documento, calcula el destino con `getPostUrl()`/`getPageUrl()`/`/` (nunca desde un parámetro de la solicitud), habilita Draft Mode y redirige — verificado en vivo: sin secreto → 401; secreto correcto + Post publicado real → 307 a la URL canónica exacta (`/entretenimiento/festival-musica-independiente`), con `set-cookie: __prerender_bypass=...; HttpOnly` confirmando que Draft Mode se habilitó de verdad (AC-PREVIEW-005, AC-SEC-007)
- [x] 10.3 Crear `src/app/api/preview-exit/route.ts`: deshabilita Draft Mode y redirige a `/` — verificado en vivo end-to-end: misma sesión de cookies, tras `/api/preview-exit` el contenido vuelve a ser el publicado (`GET /api/preview-exit` → 307 a `/`)
- [x] 10.4 Configurar `admin.preview` en `Posts.ts`/`Pages.ts`/`Home.ts` (`generatePostPreviewURL`/`generatePagePreviewURL`/`generateHomePreviewURL`, `src/lib/preview/generate-preview-url.ts`) — verificado en vivo para Post, Home y Page (con una Page de prueba real creada con credenciales de Admin) — los 3 tipos redirigen correctamente y muestran su contenido en Draft. **Hallazgo durante implementación**: `PREVIEW_SECRET` no estaba en `compose.yaml` (`.env.example` la documentaba pero el servicio `app` nunca la reenviaba) — agregada con default dev-only, contenedor recreado y confirmado (AC-PREVIEW-003)
- [x] 10.5 Verificar que Categories no exponen ningún flujo de Preview — verificado en vivo: `GET /api/preview?...&collection=categories&id=6` → 404 (rechazado explícitamente por `resolvePreviewDocument`, que solo permite `posts`/`pages`/`home`)
- [x] 10.6 Verificar en vivo que Draft Mode habilitado nunca sirve una respuesta cacheada públicamente ni contenido en Draft a tráfico público — verificado en vivo, completo, con credenciales de Admin reales: la petición pública (sin cookie) siempre vio el contenido publicado mientras una sesión con Draft Mode habilitado veía el contenido en Draft, para Home, Article y Page. **Hallazgo real corregido durante esta verificación** (ver tarea 10.7): sin ese fix, Draft Mode redirigía correctamente pero renderizaba el contenido publicado, no el Draft (AC-CACHE-006)
- [x] 10.7 (hallazgo durante 10.6) Habilitar Draft Mode solo hace bypass de `fetch`/`unstable_cache` - no le dice a Payload que debe devolver la versión en Draft. Se creó `src/lib/preview/draft-documents.ts` (`findDraftHome()`/`findDraftPostBySlug()`/`findDraftPageBySlug()`, `draft: true` + `overrideAccess: false` + usuario de la sesión) y se conectó a Home/Article/`[slug]` (Page): cada página verifica `draftMode().isEnabled` antes de decidir si usa la función pública del DAL o la lectura consciente de Draft. Se agregó también `canViewDraftRevision()` (`src/lib/preview/authorize-draft-content.ts`) como defensa en profundidad explícita para Posts (verificado en vivo que Payload ya resuelve esto correctamente por sí solo; el chequeo lo hace explícito y verificable en código, no corrige una fuga confirmada) — re-verificado en vivo de extremo a extremo tras el fix (login real → Preview → contenido en Draft visible → Exit → contenido publicado de nuevo), para Home, Article y Page

## 11. `/llms.txt`

- [x] 11.1 Crear `src/app/llms.txt/route.ts` + `src/lib/seo/llms-txt.ts` (H1 identidad del sitio, blockquote resumen desde `SiteSettings`, H2 Categorías, H2 Últimas noticias, H2 Páginas), `Content-Type: text/markdown; charset=utf-8` — verificado en vivo: `GET /llms.txt` devuelve la estructura v2 completa y correcta con los datos reales del seed (AC-LLM-001, AC-LLM-002)
- [x] 11.2 Reutilizar proyecciones estrechas del DAL (`getPublishedPostsForLlms()`/`getPublishedPagesForLlms()`, nuevas, con `select` explícito) — verificado por inspección que ninguna carga `content`
- [x] 11.3 Escapar caracteres especiales de Markdown (`[`, `]`, `(`, `)`, saltos de línea) en título/extracto antes de interpolar — verificado en vivo con un título de prueba real (`Test [brackets] (parens) and\nnewline`): el documento generado escapó los corchetes/paréntesis y colapsó el salto de línea, sin romper la estructura
- [x] 11.4 Verificar que solo Categorías públicas y Posts/Pages publicados aparecen — confirmado por el uso de `findPublished()` (mismo boundary `overrideAccess: false` + `_status: published`) en ambas proyecciones nuevas (AC-LLM-003)
- [x] 11.5 Verificar que todos los enlaces son absolutos (`getAbsoluteUrl()`) y que los Posts usan `primaryCategory` — verificado en vivo en la salida real de `/llms.txt` (AC-LLM-004)
- [x] 11.6 Acotar el listado de Posts recientes a 25 (`RECENT_POSTS_LIMIT`, dentro del rango 20-30 recomendado) — verificado en vivo: exactamente los 25 Posts publicados del seed aparecen, ninguno de más (AC-LLM-005)
- [x] 11.7 Envolver el ensamblado con el tag `llms` (`unstable_cache`); ya incluido en los hooks de invalidación de Posts/Categories/Pages/SiteSettings de las secciones 3/9 (sin cambios adicionales) — verificado en vivo: editar el excerpt de un Post real se refleja en `/llms.txt` en la siguiente request (AC-LLM-006)
- [x] 11.8 Agregar `rel="describedby"` apuntando a `/llms.txt` en el layout público raíz — la API de Metadata tipada no tiene campo para esta relación (`alternates` solo modela `canonical`/`languages`/`media`/`types`), así que se usó un `<head>` manual con `<link>`, el escape hatch que el propio framework deja para esto — verificado en vivo en el HTML servido de `/`
- [x] 11.9 Confirmar que `robots.ts` (sección 7.2) no bloquea `/llms.txt` — confirmado por inspección: la lista `disallow` de producción es `['/admin', '/api', '/api/preview', '/api/preview-exit']`, `/llms.txt` no está incluida

## 12. Actualización narrow del Master Spec

- [x] 12.1 Agregar a `docs/60-segundos-spec.md`: línea de roadmap de Fase 8 ("LLM / Agent Discoverability vía `/llms.txt`"), nueva subsección §41.6, `AC-LLM-001` a `AC-LLM-006`, y `Page.slug` como alcance aprobado del ciclo de vida de Redirects en §17.1 — verificado que ningún otro requirement se modificó (`openspec validate --strict` sigue pasando)

## 13. Documentación

- [x] 13.1 Agregar sección a `docs/FRONTEND-ARCHITECTURE.md` (reemplaza su actual línea "Fuera de alcance de esta Fase") documentando preview/cache/redirects/SEO/llms.txt — referencia `docs/SEO-AND-CACHING.md` para el detalle técnico completo, sin duplicar el Master Spec
- [x] 13.2 Crear `docs/SEO-AND-CACHING.md`: precedencia de metadata, shapes de JSON-LD, matriz de invalidación, ciclo de vida de redirects, `/llms.txt` — refleja la implementación real (nombres de archivo/función reales), incluidos los dos hallazgos de implementación (guard `server-only` removido de 3 módulos, `PREVIEW_SECRET` agregada a `compose.yaml`)
- [x] 13.3 Actualizar `README.md`: estado del proyecto ahora dice "Phase 8" completada; `PREVIEW_SECRET`/`NEXT_PUBLIC_SITE_URL` documentadas con su comportamiento real (requeridas en tiempo de uso, no de arranque); confirmado que ningún comando/variable documentada es inventada (coincide con `compose.yaml`/`env.ts` reales)

## 14. Graphify

- [x] 14.1 Correr `graphify update .` tras completar la implementación — verificado: 2417 nodos/3544 aristas/195 comunidades; `graphify query "preview draft mode cache redirects llms.txt"` encuentra `llms-txt.ts`, `invalidate.ts`, `canonical.ts`, `sitemap.ts`, los nuevos DAL functions y las specs del change

## 15. Validación estática y de build

- [x] 15.1 Correr `pnpm typecheck` y confirmar que pasa sin errores — limpio en el estado final completo
- [x] 15.2 Correr `pnpm lint` y confirmar que pasa sin errores — limpio en el estado final completo
- [x] 15.3 Correr `pnpm build` y confirmar que `/sitemap.xml`, `/robots.txt`, `/llms.txt` y las rutas de preview aparecen en la salida de build — verificado dentro del contenedor Docker real (`docker compose exec app pnpm build`): build de producción exitoso, con la clasificación esperada de rutas — `/sitemap.xml`/`/robots.txt` estáticas (○), `/llms.txt`/`/api/preview`/`/api/preview-exit`/`/[category]`/`/[category]/[post]` dinámicas (ƒ), `/` ahora estática (○) tras remover `force-dynamic`
- [x] 15.4 Levantar `docker compose up --build` (ya corriendo; se recreó el contenedor `app` para recoger `PREVIEW_SECRET` en la tarea 10.4) y confirmar que `/sitemap.xml`, `/robots.txt`, `/llms.txt` responden 200 y que un Preview real funciona — verificado en vivo contra el contenedor real en las secciones 7/10/11. **No verificado**: arrancar el servidor de producción (`next start`) dentro de este mismo contenedor compartido para probar el comportamiento real de ISR/`revalidateTag` en producción (no solo en `next dev`) — se evitó deliberadamente por el riesgo de interferir con el servidor de desarrollo ya corriendo en el mismo contenedor (mismo puerto 3000); recomendado como verificación aislada antes de archivar el change si se considera crítico

## 16. Verificación end-to-end (datos reales)

- [x] 16.1 Preview — **verificado en vivo, completo, con credenciales de Admin reales**: Post/Page/Home publicados responden normal (200); Draft de cada uno no visible sin autorización; Preview autorizado (autor o Admin) muestra el Draft real para los 3 tipos; `writer.dos` (no autor) ve la versión publicada, nunca el Draft; salir de Preview restaura el estado publicado. Bug real encontrado y corregido durante esta verificación: ver tarea 10.7
- [x] 16.2 SEO — **verificado en vivo, completo**: metadata correcta en Home/Category/Article/Page (título/descripción/canonical/OG, incluida una Page real de prueba); canonical de Article idéntico a la URL de `ShareActions`; JSON-LD válido y sin `email`/`role`/`active` del autor. El caso de una URL con `additionalCategory` sigue cubierto estructuralmente por la corrección canónica de Fase 7 (no re-probado aquí, sin cambios en esta fase)
- [x] 16.3 Sitemap/robots — **verificado en vivo**: sitemap contiene exactamente las Categories/Posts/Pages publicados reales (incluida una Page de prueba agregada y removida durante la verificación), ningún Draft; robots bloquea todo en desarrollo (`NODE_ENV !== 'production'`); el caso de producción se confirmó indirectamente por inspección de código y por la clasificación de rutas del build de producción real (tarea 15.3), no con una request real contra un servidor en modo producción
- [x] 16.4 Redirects — **verificado en vivo, extensamente, para Post/Category/Page**: cambio de slug de Post crea el redirect esperado (301 almacenado → 308 servido); cambio de slug de Category cascada correctamente a los 6 Posts publicados afectados; cambio de slug de Page (con credenciales de Admin) crea el redirect esperado; un ciclo real (rename + revert) desactiva el redirect obsoleto y crea el inverso (bug real encontrado y corregido en la tarea 9.2); una Category con un redirect apuntando a su propia URL vigente nunca la eclipsa
- [x] 16.5 Cache — **verificado en vivo, completo, con credenciales de Admin reales**: publicar/actualizar un Post invalida los tags esperados (contenido reflejado de inmediato en Article/sitemap/llms.txt); Navigation/Footer/SiteSettings cada uno invalida su propio tag sin afectar a los demás (verificado explícitamente con mutaciones reales vía REST a través del servidor Next.js real, no la CLI); `SiteSettings` además confirmado que solo revalida `llms` cuando cambian `branding`/`seo` (el cambio de `branding.siteName` se reflejó tanto en Home como en `/llms.txt`)
- [x] 16.6 `/llms.txt` — **verificado en vivo, completo**: estructura v2 válida, solo contenido publicado, URLs absolutas con `primaryCategory`, acotado a 25, se revalida ante cambios reales de Post, caracteres especiales (`[`, `]`, `(`, `)`, salto de línea) no corrompen el documento
- [x] 16.7 Seguridad — **auditado**: `grep -rn "overrideAccess: true"` confirma que todo uso vive exclusivamente en `references.ts`/`create-historical-redirect.ts`/`redirects.ts`/los hooks de redirect-lifecycle/cache-invalidation — nunca en `public-query.ts`, en ningún componente, ni en `/api/preview/route.ts` (que solo lo menciona en un comentario diciendo que nunca lo usa); confirmado que `/api/preview` solo lee `secret`/`collection`/`id` de la query string — el destino de redirect siempre se calcula desde el documento resuelto, nunca de un parámetro
