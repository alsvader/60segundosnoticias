## 1. Tokens de color y eliminación de dark mode

- [x] 1.1 Reescribir `src/app/globals.css`: declarar `--brand-red*`, `--ink-*`, `--paper-*`, `--border-*`, `--success/warning/error/info` en `@theme`/`:root`, y remapear los slots semánticos de shadcn (`--background`, `--foreground`, `--primary`, `--card`, `--border`, `--radius`, etc.) a estos tokens. Verificar visualmente que `HomePage` (boilerplate actual) cambia de la paleta neutra a paper/rojo (AC-DESIGN-001, AC-DESIGN-002).
- [x] 1.2 Eliminar el bloque `.dark` y `@custom-variant dark` de `globals.css`. Verificar con `grep -n "dark" src/app/globals.css` que no queda ninguna regla de modo oscuro activa.
- [x] 1.3 Ajustar la escala de radius a `4, 8, 12, 16, 999` y confirmar que `Button` (sin editar su código, salvo eliminar `dark:`) renderiza con el radius y color esperados tras 1.1.

## 2. Tipografía

- [x] 2.1 Configurar `next/font/google` para Oswald e Inter (pesos necesarios para display/UI) en el layout del frontend, exponiendo `variable: '--font-display'` / `variable: '--font-body'`.
- [x] 2.2 Conectar `--font-display`/`--font-body` en `@theme` de `globals.css` y aplicar `font-body` como fuente base de `html`/`body`. Verificar en devtools que el texto renderiza con Inter por defecto y Oswald donde se aplique explícitamente (AC-DESIGN-003, AC-DESIGN-004).
- [x] 2.3 Definir las utilidades/tokens de escala tipográfica (Display XL, Article H1, H2, H3, Section Heading, Body, Lead, Metadata) con sus rangos responsivos del Master Spec §46.

## 3. Category theme system

- [x] 3.1 Añadir en `globals.css` los 10 bloques `[data-cat-theme="<key>"]` (una entrada por cada key de `CATEGORY_THEME_KEYS`) definiendo `--cat-accent`, `--cat-accent-fg`, `--cat-soft`, `--cat-soft-fg`, `--cat-border`, con foreground explícito por key (blanco donde el accent es oscuro/saturado, `ink-950` donde el accent es claro como `yellow`).
- [x] 3.2 Verificar contraste texto/fondo de `--cat-accent-fg` sobre `--cat-accent` y `--cat-soft-fg` sobre `--cat-soft` para las 10 keys, con atención particular a `yellow`, `green`, `cyan` (AC-A11Y-006). Registrar los valores de contraste verificados. — Calculado por WCAG relative luminance para las 10 keys; tabla completa en `docs/DESIGN-SYSTEM.md`. Hallazgo: `pink` (#E6008D, hex aprobado, sin cambios) no alcanza 4.5:1 contra blanco ni ink-950 en texto normal (mejor opción 4.27:1) — restricción documentada y **verificada como no violada por ningún componente**: `CategoryBadge` `default`/`compact` fue rediseñado (ver 7.2) para usar `--cat-soft`/`--cat-soft-fg` en vez de `--cat-accent`/`--cat-accent-fg`, y `overlay` usa `--cat-overlay-bg` (oscurecido) + `paper-50` fijo — ningún componente pinta texto pequeño directamente sobre el accent saturado. Segundo hallazgo (no limitado a pink): el accent saturado puro falla el mínimo no-textual de 3:1 contra `paper-50` para `blue`/`orange`/`green`/`yellow` — afectaba al icono de `CategoryCard`, corregido usando `--cat-strong` (ver 7.3). Ambas tablas de verificación en `docs/DESIGN-SYSTEM.md`.
- [x] 3.3 Crear el módulo de mapeo `category icon key → componente Lucide` (fuera de `src/payload/`), cubriendo las 6 keys existentes (`newspaper, video, plane, star, popcorn, dots`). Verificar que `src/lib/constants/category-icon-keys.ts` y `category-theme-keys.ts` siguen sin importar React/Tailwind/Lucide (`grep` de imports). — `src/components/editorial/category-icon-map.tsx`; grep confirma cero imports en ambas constantes.
- [x] 3.4 Confirmar (test manual o unitario) que una category theme key no reconocida cae a un theme por defecto documentado, no a un color arbitrario. — `resolveCategoryThemeKey()` en `src/lib/editorial/category-theme.ts`, fallback `purple`.

## 4. Layout primitives

- [x] 4.1 Implementar `src/components/layout/Container.tsx` con max-width ~1360-1440px y gutters responsivos (16-20 / 24-32 / 40-48). Verificar en los tres breakpoints clave que no hay scroll horizontal accidental (AC-RESP-001, AC-RESP-002). — Implementado en `container.tsx` (max 1400px, gutter 16/28/44px vía media queries). Verificado por render SSR + inspección de clases; sin herramienta de navegador headless disponible en este entorno para un screenshot pixel-a-pixel (ver nota de sesión).
- [x] 4.2 Documentar/exponer los helpers de ancho legible de artículo (~680-760px) y wide media (~1000-1120px) como utilidades o constantes reutilizables. — `.measure-article` (45rem) / `.measure-wide` (66rem) en `globals.css`.

## 5. Texturas

- [x] 5.1 Exponer una utilidad reutilizable para `public/textures/paper-grain.webp` sobre el fondo `paper`, en opacidad sutil. Verificar legibilidad de texto de muestra sobre el fondo texturizado (AC-DESIGN-005). — `.texture-paper-grain`; implementada como pseudo-elemento `::before` con `z-index:-1` para que `opacity` nunca afecte el texto del propio elemento (no solo la textura).
- [x] 5.2 Exponer una utilidad reutilizable para `public/textures/newspaper-pattern.webp` en opacidad 3-7%, documentada para uso en Hero/secciones decorativas de fases futuras, sin construir dicho Hero en este change. — `.texture-newspaper-pattern`, mismo patrón contenido.

## 6. Primitivos shadcn

- [x] 6.1 Confirmar que `Button` re-tematizado (tras Sección 1) cumple AC-UI-003/AC-UI-004 (no conserva apariencia genérica) y AC-DESIGN-007 (no parece template shadcn) mediante inspección visual de sus variantes/tamaños existentes. — Verificado por render SSR (fondo/color de marca aplicado); también se eliminaron las clases `dark:` heredadas del CLI y se corrigió el touch target de todas las variantes de tamaño (`icon*` y `xs`/`sm`/`default` de texto) a 44px real/efectivo (ver 8.4).
- [x] 6.2 Para cada componente editorial de la Sección 7: evaluar si requiere un primitivo shadcn adicional a `Button`. Instalar únicamente los primitivos con consumidor demostrado dentro de este change (no ejecutar `shadcn add --all`). Documentar en `docs/DESIGN-SYSTEM.md` cuáles se instalaron y por qué componente. — Ningún primitivo adicional fue necesario; `Pagination` reutiliza `Button`. Documentado en `docs/DESIGN-SYSTEM.md`.

## 7. Componentes editoriales presentacionales

- [x] 7.1 Definir el contrato `ArticleCardData` (frontend-only) e implementar `ArticleCard` contra ese contrato, sin importar `payload-types.ts` ni Payload. Verificar con datos de ejemplo locales (fixture), no con Payload (AC-COMP-001, AC-COMP-002, AC-COMP-003, AC-COMP-004). — Verificado con fixture temporal renderizada vía SSR (curl), luego removida de `page.tsx`. Foco visible explícito añadido (mismas clases que `CategoryCard`).
- [x] 7.2 Implementar `CategoryBadge` con variantes `default`, `compact`, `overlay`, consumiendo `data-cat-theme`. — `default`/`compact` usan `--cat-soft`/`--cat-soft-fg` (nunca `--cat-accent`/`--cat-accent-fg`) para que las 10 keys cumplan contraste de texto sin excepción; `overlay` usa `--cat-overlay-bg` + `paper-50` fijo. Ver hallazgo/fix de contraste en 3.2.
- [x] 7.3 Implementar `CategoryCard` mostrando icon (vía el mapeo de la tarea 3.3), nombre uppercase, descripción y accent de theme, con hover `translateY` restringido (~-3px), foco visible explícito y sin surface saturada (AC-COMP-005, AC-COMP-006). — El icono usa `--cat-strong` (no `--cat-accent`) tras el hallazgo de contraste no-textual en 3.2; la barra de acento bajo el nombre sigue usando `--cat-accent` puro (decorativa, 1.4.11 no aplica). Foco: `outline-none focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50`.
- [x] 7.4 Implementar `ArticleMetadata` (autor, fecha, reading time, slot de categoría) sin acceso a datos.
- [x] 7.5 Implementar `SectionHeader` con props `title`/`eyebrow`/`action`/`theme` y nivel de encabezado configurable. — `theme` (category theme key opcional) tiñe el eyebrow/accent vía `data-cat-theme`; `as` controla el nivel de heading semántico (AC-A11Y-005).
- [x] 7.6 Implementar `Breadcrumbs` y `Pagination` como componentes presentacionales dirigidos por props. — Links de `Breadcrumbs` con foco visible explícito (`focus-visible:ring-3 focus-visible:ring-ring/50`); `Pagination` reutiliza `Button`, que ya trae foco propio.
- [x] 7.7 Implementar `ResponsiveMedia` envolviendo `next/image`, con `alt` obligatorio, sizing responsivo, aspect-ratio y `priority` opcional, sin fetch a Payload Media. — Verificado end-to-end: `/_next/image` devuelve HTTP 200 para un asset local de prueba.
- [x] 7.8 Revisar cada componente de esta sección y confirmar (por inspección de imports) que ninguno importa Payload, la Local API ni realiza `fetch`. — `grep -rn "payload\|fetch(" src/components/editorial/` sin coincidencias reales (solo un comentario que menciona `payload-types.ts` como lo que deliberadamente evita).

## 8. Accesibilidad y motion

- [x] 8.1 Definir los tokens de motion (`--motion-fast: 120ms`, `--motion-normal: 200ms`, `--motion-slow: 320ms`) usando `tw-animate-css`/utilidades CSS existentes, sin nueva dependencia.
- [x] 8.2 Envolver las transiciones de hover/scale de los componentes de la Sección 7 en `@media (prefers-reduced-motion: reduce)` para desactivarlas o reducirlas (AC-A11Y-008). — Implementado como un reset global único en `globals.css` en vez de duplicar la media query por componente; cubre automáticamente cualquier transición nueva.
- [x] 8.3 Verificar navegación por teclado y foco visible en `Button`, `CategoryBadge`/`CategoryCard` (si son interactivos) y controles icon-only, confirmando `aria-label` donde falte texto visible (AC-A11Y-001, AC-A11Y-002, AC-A11Y-003). — `Button` conserva sus estados `focus-visible` existentes. `CategoryCard`/`ArticleCard` (`<Link>`) y los links de `Breadcrumbs` ya no dependen solo del `outline-ring/50` global heredado (`@layer base`) ni del outline nativo del navegador: llevan `focus-visible:ring-3 focus-visible:ring-ring/50` explícito (más `focus-visible:border-ring` en las cards), con los mismos tokens que `Button`, verificado en la hoja de estilos compilada (`grep` sobre el CSS de `pnpm dev`, ver 10.1). `CategoryBadge` no es interactivo (nunca se usa como control por sí solo, siempre anidado dentro de un `<Link>` padre) — no lleva foco propio, correctamente, siguiendo "no agregar focus ring a elementos no interactivos". `Pagination` (icon-only) lleva `aria-label`.
- [x] 8.4 Verificar manualmente touch target mínimo de 44px en los tamaños interactivos usados en mobile (AC-A11Y-007). — Hallazgo inicial: las variantes `icon/icon-sm/icon-xs/icon-lg` de `Button` (32/28/24/36px) no llegaban a 44px; corregido con un `::after` invisible expandido en las 4 direcciones (seguro porque estos botones nunca están adyacentes entre sí en ningún consumidor actual — `Pagination` los separa con un `<span>` de texto). Hallazgo de re-verificación: los tamaños de **texto** `xs`/`sm`/`default` (24/28/32px de alto) tampoco llegaban a 44px, y la spec no limita el requisito a los tamaños icon-only. Corregido con una técnica distinta, elegida específicamente para no arriesgar solapamiento en un `button-group` horizontal (donde los botones se tocan, gap=0): `min-w-11` real (44px, sin riesgo — participa del layout flex/gap normalmente) + `::after` invisible expandido **solo verticalmente** (`inset-x-0` + `-inset-y-Npx`, cero expansión horizontal, por lo que dos botones de texto uno junto al otro nunca pueden solaparse, con cualquier gap incluido gap=0). Verificado en la hoja de estilos compilada: `min-w-11 → min-width: calc(var(--spacing) * 11)` = 44px; `after:inset-x-0 → inset-inline: calc(var(--spacing) * 0)` = 0px (cero expansión horizontal, confirmado, no solo asumido). `lg` (36px) no estaba en el alcance solicitado y queda sin cambios.

## 9. Documentación

- [x] 9.1 Crear `docs/DESIGN-SYSTEM.md` documentando tokens, tipografía, category theme mapping, convenciones de componentes y reglas de uso de shadcn (qué primitivos existen y por qué).
- [x] 9.2 Añadir referencia a `docs/DESIGN-SYSTEM.md` desde `AGENTS.md`/`CLAUDE.md` (root) siguiendo el patrón ya usado para referenciar `docs/60-segundos-spec.md`.

## 10. Validación final

- [x] 10.1 Ejecutar `pnpm typecheck` y `pnpm lint`, verificar que ambos pasan sin errores nuevos. — Ambos pasan limpio, re-ejecutados tras la ronda de fixes de `/opsx:verify` (touch target, foco visible, contraste `CategoryBadge`/`CategoryCard`).
- [x] 10.2 Ejecutar `pnpm build` y verificar que la build de producción completa sin errores. — Build de producción completa sin errores (Turbopack), re-ejecutado tras la misma ronda de fixes.
- [x] 10.3 Verificar manualmente que `src/app/(payload)/admin` (panel de Payload) no se ve afectado visualmente por los cambios en `globals.css` (no lo importa, pero se confirma por inspección visual tras el build). — Confirmado por inspección estática: `src/app/(payload)/layout.tsx` no importa `globals.css` (único import está en `src/app/(frontend)/layout.tsx`). No se realizó una verificación visual en navegador del panel de administración (ver nota de sesión sobre herramienta de navegador no disponible).
- [x] 10.4 Ejecutar `graphify update .` para refrescar el grafo con los componentes y módulos nuevos, dado que esta change altera materialmente `src/components/` y `src/app/globals.css`. — Ejecutado (dos pasadas, la segunda tras el ajuste de `SectionHeader`): grafo final 860 nodes, 1088 edges, 91 communities.
- [x] 10.5 Confirmar en `docs/60-segundos-spec.md` §84 (Acceptance Criteria) que los AC-* referenciados en las specs de esta change (AC-GEN-009/010, AC-UI-001-005, AC-COMP-001-006, AC-RESP-001/002, AC-A11Y-001-008, AC-DESIGN-001-008 salvo AC-DESIGN-008 que depende de fases futuras) quedan cumplidos por el alcance de Fase 4, sin marcar como PASS ningún criterio de nivel de página completa. — Revisado; ningún AC de nivel de página (Home/Categoría/Artículo) se marca satisfecho por este change.

---

## 11. Fixes de la ronda de `/opsx:verify` (WARNING → resuelto)

El primer `/opsx:verify` encontró 2 WARNING y pidió auditar un caso (pink). Resultado de esta ronda:

- [x] 11.1 **Touch target en variantes de texto.** El fix inicial (tarea 8.4) solo cubría `icon*`. Se extendió a `xs`/`sm`/`default` con `min-w-11` (real, sin riesgo) + `::after` vertical-only (`inset-x-0`, cero expansión horizontal) — ver 8.4 para el detalle y la verificación en CSS compilado.
- [x] 11.2 **Foco visible explícito.** Agregado a `CategoryCard`, `ArticleCard` y los links de `Breadcrumbs` con los mismos tokens que `Button` (`--ring`), vía `:focus-visible`, no solo el `outline-ring/50` heredado globalmente. `CategoryBadge`/`SectionHeader`/`ArticleMetadata` confirmados no-interactivos — sin foco añadido, correctamente.
- [x] 11.3 **Auditoría del theme `pink`.** El hex `#E6008D` no se modificó. Se encontraron y corrigieron dos violaciones reales de la restricción documentada: `CategoryBadge` `default`/`compact`/`overlay` pintaban texto pequeño sobre el accent saturado (fallaba para `pink`, único theme sin ninguna opción ≥4.5:1) — movidos a `--cat-soft`/`--cat-soft-fg`/`--cat-overlay-bg`. Hallazgo adicional durante la misma auditoría (no limitado a pink): el icono de `CategoryCard` sobre `paper-50` fallaba el mínimo no-textual 3:1 para `blue`/`orange`/`green`/`yellow` — corregido con `--cat-strong`. Todas las tablas de contraste (antes/después) están en `docs/DESIGN-SYSTEM.md`.
- [x] 11.4 Re-ejecutados `pnpm typecheck`/`lint`/`build` (limpio) tras los fixes; verificado el CSS compilado (`pnpm dev` + `curl`) para confirmar que las clases Tailwind nuevas (`min-w-11`, `after:inset-x-0`, `after:-inset-y-*`, `--cat-strong`, `--cat-overlay-bg`) resuelven a los valores exactos esperados, no solo que compilan.
- [x] 11.5 `docs/DESIGN-SYSTEM.md` actualizado (tokens, contraste, touch target, foco). `graphify update .` re-ejecutado tras esta ronda (902 nodes, 1134 edges, 105 communities).

**Nota de sesión — limitación de verificación visual:** este entorno no tiene `chromium-cli`/Playwright instalado, y no se instaló para esta change (evitar una dependencia de testing nueva no solicitada). La verificación de UI se hizo por: (a) `pnpm build`/`typecheck`/`lint` sin errores en cada ronda, (b) montaje temporal de todos los componentes en `HomePage` en la ronda de implementación inicial, servidor `pnpm dev` real, y verificación vía `curl` del HTML y CSS renderizados en servidor (clases/valores Tailwind exactos, los `data-cat-theme` esperados presentes, `/_next/image` sirviendo assets con HTTP 200), y (c) verificación matemática de contraste (WCAG, luminancia relativa) tanto para texto-sobre-accent como para el mínimo no-textual 3:1. Esto confirma que el árbol de componentes monta, renderiza y compila a los valores CSS exactos pretendidos sin errores de servidor, pero no sustituye una inspección visual real en navegador (colores exactos tal como los percibe un ojo humano, hover, foco, breakpoints). Si se dispone de Playwright/navegador, se recomienda una pasada visual antes de archivar.
