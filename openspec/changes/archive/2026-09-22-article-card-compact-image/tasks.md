## 1. Implementación

- [x] 1.1 En `src/components/editorial/article-card.tsx`, variante `compact`: tarjeta `items-stretch p-0`, imagen `w-28 sm:w-32 shrink-0 self-stretch` sin `rounded-lg`, y bloque de texto con `py-3 pr-3 self-center` (más `pl-3` si no hay imagen).

## 2. Verificación

- [x] 2.1 `pnpm test` (typecheck, lint, importmap, unit) pasa.
- [x] 2.2 `graphify update .` refresca el grafo.
