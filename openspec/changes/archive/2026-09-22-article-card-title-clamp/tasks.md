## 1. Implementación

- [x] 1.1 En `src/components/editorial/article-card.tsx`: título con `line-clamp-3` (`default`) / `line-clamp-2` (`compact`) y `break-words`; bloque de texto con `min-w-0`.

## 2. Verificación

- [x] 2.1 `pnpm test` (typecheck, lint, importmap, unit) pasa.
- [x] 2.2 `graphify update .` refresca el grafo.
