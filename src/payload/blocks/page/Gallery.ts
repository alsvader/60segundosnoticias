// Reuses the exact same controlled structure as the article GalleryBlock,
// per spec: Pages' Gallery block must match the article block's structure.
export { GalleryBlock as Gallery } from '../article/GalleryBlock.ts'
