// Reuses the exact same controlled provider structure as the article
// VideoBlock, per spec: Pages' Video block must match the article block.
export { VideoBlock as Video } from '../article/VideoBlock.ts'
